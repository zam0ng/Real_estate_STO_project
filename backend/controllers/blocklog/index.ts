import { Request, Response } from "express";
import { db } from "../../models";

export const RealEstateNames = async (req: Request, res: Response) => {
  try {
    //
  } catch (error) {
    console.error(error);
  }
};

export const setTx_block = async (req: Request, res: Response) => {
  try {
    //
  } catch (error) {
    console.error(error);
  }
};

export const setTx_receipt = async (req: Request, res: Response) => {
  try {
    //
  } catch (error) {
    console.error(error);
  }
};
